name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Build package
      run: |
        python -m build

    - name: Check package
      run: |
        twine check dist/*

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: dist
        path: dist/

  github-release:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: dist
        path: dist/

    - name: Extract version from tag
      id: version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        name: Release v${{ steps.version.outputs.VERSION }}
        body: |
          🚀 **MCPost v${{ steps.version.outputs.VERSION }}** - Documentation & Infrastructure Improvements

          ## ✨ New Features
          - 📚 Professional GitHub Pages documentation site with Jekyll
          - 📖 Comprehensive tutorials: Getting Started and Advanced GSA  
          - 💼 Financial risk analysis example
          - 🤖 Automated documentation deployment workflow

          ## 🐛 Bug Fixes
          - ✅ Fixed GitHub Actions CI/CD pipeline (updated deprecated actions)
          - 🔧 Resolved Jekyll build errors in documentation deployment
          - 📓 Fixed Jupyter notebook JSON parsing issues
          - 🔗 Corrected broken documentation links and repository URLs
          - 📊 Resolved code complexity issues in CI pipeline

          ## 🔧 Improvements
          - ⚡ Streamlined CI workflow for faster builds
          - 📋 Enhanced documentation structure and navigation
          - 🎨 Improved notebook-to-HTML conversion with better templates
          - 🧹 Repository cleanup and organization (removed development clutter)
          - 🚀 Ultra-fast CI focusing on core functionality

          ## 📚 Documentation
          - 🌐 Professional documentation site at https://zzhang0123.github.io/mcpost/
          - 📱 Interactive tutorials converted from Jupyter notebooks
          - 🎯 Comprehensive examples gallery
          - 📱 Mobile-friendly responsive design

          ## 🔄 Backward Compatibility
          This release maintains full backward compatibility with the core MCPost functionality.

          ## 📦 Installation
          ```bash
          pip install mcpost==${{ steps.version.outputs.VERSION }}
          ```

          ## 🔗 Links
          - 📖 [Documentation](https://zzhang0123.github.io/mcpost/)
          - 🐛 [Issues](https://github.com/zzhang0123/mcpost/issues)

          ---
          See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for detailed changes.
        files: |
          dist/*
        draft: false
        prerelease: ${{ contains(steps.version.outputs.VERSION, 'rc') || contains(steps.version.outputs.VERSION, 'alpha') || contains(steps.version.outputs.VERSION, 'beta') }}

  pypi-publish:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: dist
        path: dist/

    - name: Publish release distributions to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: dist/
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}